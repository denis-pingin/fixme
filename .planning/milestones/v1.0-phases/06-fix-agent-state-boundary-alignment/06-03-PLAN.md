---
phase: 06-fix-agent-state-boundary-alignment
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - .claude/skills/fixme/SKILL.md
  - .claude/skills/fixme/references/state-machine.md
  - .planning/phases/05-verification-close-loop/05-01-SUMMARY.md
autonomous: true
requirements:
  - STAT-01
  - FIXR-01

must_haves:
  truths:
    - "SKILL.md does NOT transition `queued -> investigating` (investigation-agent owns this)"
    - "SKILL.md does NOT transition `investigating -> fixing` (the `fixing` state no longer exists)"
    - "SKILL.md does NOT transition `fixing -> verifying` (verifier owns `implementing -> verifying`)"
    - "SKILL.md transitions `verifying -> done` after successful commit"
    - "SKILL.md crash handler reads actual ticket state from disk and transitions [current state] -> failed"
    - "state-machine.md documents 9 states with the new transition matrix"
    - "state-machine.md has no references to `fixing` state"
    - "05-01-SUMMARY.md acknowledges the architectural deviation"
  artifacts:
    - path: ".claude/skills/fixme/SKILL.md"
      provides: "Dispatch loop with agent-owned transitions, state-agnostic crash handler"
      contains: "verifying -> done"
    - path: ".claude/skills/fixme/references/state-machine.md"
      provides: "Complete state machine documentation with 9 states"
      contains: "researching"
    - path: ".planning/phases/05-verification-close-loop/05-01-SUMMARY.md"
      provides: "Corrected deviation acknowledgment"
  key_links:
    - from: ".claude/skills/fixme/SKILL.md"
      to: ".claude/skills/fixme/agents/investigation-agent.md"
      via: "Task dispatch -- agent now owns queued->investigating"
      pattern: "investigation-agent.md"
    - from: ".claude/skills/fixme/SKILL.md"
      to: ".claude/skills/fixme/agents/fix-agent.md"
      via: "Task dispatch -- ticket already in investigating state"
      pattern: "fix-agent.md"
    - from: ".claude/skills/fixme/SKILL.md"
      to: ".claude/skills/fixme/scripts/fixme-tools.cjs"
      via: "verifying -> done transition after commit"
      pattern: "ticket transition.*done"
---

<objective>
Rewrite SKILL.md dispatch boundaries (agents own their transitions, SKILL.md only owns terminal transitions), rewrite state-machine.md with the new 9-state model, and fix the 05-01-SUMMARY.md inaccuracy.

Purpose: Completes the state boundary alignment by updating the orchestrator and documentation. SKILL.md's dispatch loop must match the new agent-owned transition model. state-machine.md is the single source of truth for the state machine and must reflect the new design.
Output: Updated SKILL.md dispatch loop, rewritten state-machine.md, corrected 05-01-SUMMARY.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fix-agent-state-boundary-alignment/06-RESEARCH.md
@.planning/phases/06-fix-agent-state-boundary-alignment/06-01-SUMMARY.md
@.claude/skills/fixme/SKILL.md
@.claude/skills/fixme/references/state-machine.md
@.planning/phases/05-verification-close-loop/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SKILL.md dispatch loop</name>
  <files>.claude/skills/fixme/SKILL.md</files>
  <action>
Update the dispatch loop section of SKILL.md (Steps 1-6, approximately lines 170-264). The core changes:

**Step 1 (Get next ticket):** Unchanged -- still uses `ticket next`.

**Step 2 (Transition to investigating):** REMOVE this step entirely. The investigation-agent now owns the `queued -> investigating` transition. Do NOT run `ticket transition investigating` here.

**Step 3 (Dispatch investigation agent):** Update the dispatch prompt to inform the agent that it must claim state:
```
First, read ~/.claude/skills/fixme/agents/investigation-agent.md for your role instructions.

Then investigate this bug:
- Ticket file: <ticket-path>
- Project context: .fixme/project-context.yaml
- Asset directory: <ticket-folder>/assets/
- Dev server URL: <dev_server.url from project context>
```
The investigation-agent.md already has Phase 0 instructions. No extra text needed in the prompt.

**Step 4 (After investigation returns):** Update to handle the new state model.
- Read ticket state from disk (same as current)
- Check: if ticket state is still `queued`, the investigation agent crashed before transitioning. Transition `queued -> failed --reason "Investigation agent crashed without claiming state"`.
- If ticket state is `investigating` (investigation completed), proceed normally.

**Step 5 (Handle investigation result):** Major rewrite:

  - If investigation succeeded (CONFIRMED/PARTIAL):
    a. REMOVE the `investigating -> fixing` transition. The `fixing` state no longer exists.
    b. Dispatch fix-agent. The ticket is in `investigating` state. The fix-researcher (first sub-agent) will transition it to `researching`.
       ```
       First, read ~/.claude/skills/fixme/agents/fix-agent.md for your role instructions.

       You are the fix COORDINATOR. You dispatch sub-agents via Task tool -- you do NOT write code, run builds, or investigate yourself.

       Fix this bug:
       - Ticket folder: <ticket-folder-dir>
       - Project context: .fixme/project-context.yaml
       ```
    c. After fixer returns, ALWAYS read ticket state from disk.
    d. Handle fixer result:
       - **Success (status: "fixed"):**
         The ticket is now in `verifying` state (the fix-verifier transitioned `implementing -> verifying`).
         REMOVE the `fixing -> verifying` transition (it no longer exists).
         Proceed directly to commit:
         1. Read ticket to get `files_changed` and `title` from `ticket list` output
         2. Stage only fix files: `git add <file1> <file2> ...`
         3. Create commit: `git commit -m "fix: <title in lowercase>"`
         4. Capture commit hash: `git rev-parse HEAD`. Edit `commit_hash:` in ticket frontmatter.
         5. Transition `verifying -> done`: `node ~/.claude/skills/fixme/scripts/fixme-tools.cjs ticket transition <ticket-path> done`
         6. Report to user: "Fixed and committed #NNNN: <summary>"

       - **Failure (status: "failed"):**
         The ticket is in some non-terminal state (could be `researching`, `planning`, `implementing`, or `verifying` depending on where it failed).
         SKILL.md must handle cleanup:
         1. Read current ticket state from disk
         2. Get changed files: `git diff --name-only <base_commit from ticket> HEAD`
         3. If there are changed files, revert: `git checkout <base_commit> -- .` then `git clean -fd --exclude=.fixme/`
         4. Transition `[current state] -> failed --reason "<failure reason from fixer result>"`
         5. Report to user: "Failed to fix #NNNN: <reason>. Moving to next ticket."

  - If investigation was inconclusive (NOT_CONFIRMED): same as current (ask user to skip or provide more details)
  - If investigation hit BLOCKER: same as current (attempt recovery)

**Step 6 (Loop):** Unchanged.

IMPORTANT: Ensure all references to `fixing` state are removed throughout SKILL.md, not just in the dispatch loop. Search the entire file for "fixing" and remove/replace every instance.

Also update the crash handler pattern: SKILL.md must be state-agnostic when handling agent crashes. Read the actual state from disk, don't assume a specific state. The transition from any non-terminal state to `failed` is always valid per the new matrix.
  </action>
  <verify>Read SKILL.md. Confirm: (1) No `ticket transition investigating` before investigation dispatch. (2) No `investigating -> fixing` transition. (3) No `fixing -> verifying` transition. (4) `verifying -> done` transition exists after commit. (5) Failure handler reads current state and transitions [current] -> failed. (6) `grep "fixing" .claude/skills/fixme/SKILL.md` returns 0 matches for the state name (may match the word "fixing" in prose like "fixing this bug" -- check context).</verify>
  <done>SKILL.md dispatch loop uses agent-owned transitions. SKILL.md only owns terminal transitions (verifying->done, [any]->failed, investigating->skipped). Crash handler is state-agnostic. No references to `fixing` as a state name.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite state-machine.md and fix 05-01-SUMMARY.md</name>
  <files>
    .claude/skills/fixme/references/state-machine.md
    .planning/phases/05-verification-close-loop/05-01-SUMMARY.md
  </files>
  <action>
**Rewrite state-machine.md completely.** Use the research document's recommendations for state descriptions. The new document must cover:

**States table (9 states):**

| State | Description | Terminal? |
|-------|-------------|-----------|
| `queued` | Ticket created, waiting to be picked up by the dispatch loop | No |
| `investigating` | Investigation agent is analyzing the bug: reading code, reproducing in browser, forming a root cause hypothesis | No |
| `researching` | Fix-researcher is exploring the codebase around the root cause: finding relevant files, tracing code paths, identifying approach candidates | No |
| `planning` | Fix-planner is designing a step-by-step fix plan based on research findings (or re-planning after verification failure) | No |
| `implementing` | Fix-implementer is executing code changes according to the fix plan | No |
| `verifying` | Fix-verifier is checking the implementation: running build/lint/test, checking plan coverage, browser-verifying the bug is gone | No |
| `done` | Fix verified and committed. Ticket complete. | Yes |
| `failed` | Ticket could not be resolved. Reason recorded in `failure_reason` field. | Yes |
| `skipped` | Ticket skipped. Reason recorded in `failure_reason` field. | Yes |

**Transition Matrix:**

| From | Valid To States |
|------|-----------------|
| `queued` | `investigating`, `skipped`, `failed` |
| `investigating` | `researching`, `skipped`, `failed` |
| `researching` | `planning`, `failed` |
| `planning` | `implementing`, `failed` |
| `implementing` | `verifying`, `failed` |
| `verifying` | `done`, `planning` (retry), `failed` |
| `done` | _(none -- terminal)_ |
| `failed` | _(none -- terminal)_ |
| `skipped` | _(none -- terminal)_ |

**Diagram:**
```
queued -> investigating -> researching -> planning -> implementing -> verifying -> done
  |           |                |             |             |              |
  +-> skip    +-> skip         +-> fail      +-> fail      +-> fail       +-> planning (retry)
  +-> fail    +-> fail                                                    +-> fail
```

**Happy path:**
`queued -> investigating -> researching -> planning -> implementing -> verifying -> done`

**Retry path:**
`verifying -> planning` (with --reason, increments current_attempt). Then continues: `planning -> implementing -> verifying`.

**State transition ownership section (NEW):**
Document which agent owns each transition:
- investigation-agent: `queued -> investigating`
- fix-researcher: `investigating -> researching`
- fix-planner: `researching -> planning` (first) or `verifying -> planning` (retry)
- fix-implementer: `planning -> implementing`
- fix-verifier: `implementing -> verifying`
- SKILL.md: `verifying -> done`, `[any non-terminal] -> failed`, `investigating -> skipped`, `queued -> failed` (crash)

**Reason requirements table:** Update to reflect new transitions. `verifying -> planning` requires reason (not `verifying -> investigating`).

**Retry semantics:** Update trigger from `verifying -> investigating` to `verifying -> planning`. Update behavior description: "The planner starts a new plan informed by the verifier's failure feedback."

**Duration tracking:** Note that `planning` can be visited multiple times on retry. Durations are cumulative: the `prior_seconds` field preserves time from prior visits.

**Transition log example:** Update with new states:
```yaml
transitions:
  - from: queued
    to: investigating
    timestamp: "2026-02-18T14:35:00Z"
    reason: null
  - from: investigating
    to: researching
    timestamp: "2026-02-18T14:40:00Z"
    reason: null
  - from: researching
    to: planning
    timestamp: "2026-02-18T14:42:00Z"
    reason: null
  - from: planning
    to: implementing
    timestamp: "2026-02-18T14:45:00Z"
    reason: null
  - from: implementing
    to: verifying
    timestamp: "2026-02-18T14:55:00Z"
    reason: null
  - from: verifying
    to: done
    timestamp: "2026-02-18T15:00:00Z"
    reason: null
```

**Enforcement rules:** Same 5 rules but update references (no more `fixing` state, retry is `verifying -> planning` not `verifying -> investigating`).

**Timeout model:** Unchanged (agent-step timeouts, not ticket-state timeouts).

**Fix 05-01-SUMMARY.md:**
Replace the "Deviations from Plan" section. Change from:
```
## Deviations from Plan

None - plan executed exactly as written.
```
To:
```
## Deviations from Plan

Browser-verifier.md was absorbed into fix-verifier.md during execution. The original plan called for a standalone browser-verifier agent file, but during implementation the browser verification workflow was integrated into the existing fix-verifier.md as an additional phase rather than a separate agent dispatch. This simplified the verification flow by avoiding an extra sub-agent dispatch while preserving the same verification coverage.
```
  </action>
  <verify>Read state-machine.md. Confirm: (1) 9 states listed. (2) No `fixing` state anywhere. (3) Transition matrix matches the locked decision. (4) State transition ownership section exists. (5) Retry path is `verifying -> planning`. (6) Duration tracking mentions `prior_seconds`. Read 05-01-SUMMARY.md. Confirm the deviations section acknowledges the browser-verifier absorption.</verify>
  <done>state-machine.md is the complete, accurate reference for the 9-state model with agent ownership. 05-01-SUMMARY.md deviations section is corrected. No references to `fixing` state remain in any documentation.</done>
</task>

</tasks>

<verification>
1. `grep "fixing" .claude/skills/fixme/SKILL.md` -- only prose matches (e.g., "fixing this bug"), no state name usage
2. `grep "fixing" .claude/skills/fixme/references/state-machine.md` -- zero matches
3. `grep "researching" .claude/skills/fixme/references/state-machine.md` -- matches exist
4. `grep "State transition ownership" .claude/skills/fixme/references/state-machine.md` -- match exists
5. Read 05-01-SUMMARY.md deviations section -- no longer claims "None"
</verification>

<success_criteria>
- SKILL.md dispatch loop delegates queued->investigating to the agent
- SKILL.md does not reference `fixing` as a state name
- SKILL.md crash handler reads actual state from disk, transitions to failed dynamically
- state-machine.md has 9 states, correct transition matrix, agent ownership section
- 05-01-SUMMARY.md deviation acknowledged
</success_criteria>

<output>
After completion, create `.planning/phases/06-fix-agent-state-boundary-alignment/06-03-SUMMARY.md`
</output>
