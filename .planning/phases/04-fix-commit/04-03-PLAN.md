---
phase: 04-fix-commit
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - .claude/skills/fixme/SKILL.md
autonomous: true
requirements: [FIXR-02, FIXR-03, FIXR-05]

must_haves:
  truths:
    - "After investigation completes, the orchestrator transitions the ticket to fixing and dispatches the fixer agent"
    - "Orchestrator passes only the ticket folder path to the fixer -- never reads ticket bodies"
    - "Orchestrator reads ticket state from disk after fixer returns, not from fixer's return value"
    - "On fixer success: orchestrator reports result to user and advances to next queued ticket"
    - "On fixer failure: ticket is already failed (fixer handles it), orchestrator moves to next ticket"
    - "No hardcoded model: values in any SKILL.md dispatch instructions"
    - "Intake dispatch, investigation dispatch, and fixer dispatch all use new ticket-centric paths"
    - "SKILL.md references the fixer agent file in the References section"
  artifacts:
    - path: ".claude/skills/fixme/SKILL.md"
      provides: "Complete dispatch loop with investigation + fixing stages"
      contains: "fix-agent.md"
  key_links:
    - from: ".claude/skills/fixme/SKILL.md"
      to: ".claude/skills/fixme/agents/fix-agent.md"
      via: "Task tool dispatch with ticket folder path"
      pattern: "fix-agent\\.md"
    - from: ".claude/skills/fixme/SKILL.md"
      to: "fixme-tools.cjs"
      via: "ticket transition investigating -> fixing"
      pattern: "transition.*fixing"
---

<objective>
Update SKILL.md to integrate fixer agent dispatch into the dispatch loop, update all path references for the new ticket-centric directory layout, remove hardcoded model specifications from dispatch instructions, and add fixer agent references.

Purpose: This completes the orchestrator integration -- the dispatch loop now covers the full lifecycle from intake through investigation to fixing.
Output: Updated SKILL.md with fixer dispatch, new directory paths, and model inheritance.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-fix-commit/04-RESEARCH.md
@.planning/phases/04-fix-commit/04-CONTEXT.md
@.planning/phases/04-fix-commit/04-01-SUMMARY.md
@.planning/phases/04-fix-commit/04-02-SUMMARY.md
@.claude/skills/fixme/SKILL.md
@.claude/skills/fixme/agents/fix-agent.md
@.claude/skills/fixme/references/state-machine.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SKILL.md dispatch loop with fixer agent integration</name>
  <files>.claude/skills/fixme/SKILL.md</files>
  <action>
Update SKILL.md with the following changes. Read the current SKILL.md first, then make targeted edits.

**1. Dispatch Loop -- Add fixer dispatch after investigation (step 6):**

Replace the current step 6 placeholder comment `"(Phase 4 will add the investigating -> fixing transition here.)"` with the actual fixer dispatch flow:

```markdown
6. **Handle investigation result:**
   - If agent returned "Investigated #NNNN: ..." (success or partial with CONFIRMED/PARTIAL verdict):
     Report findings to user with the agent's summary.
     Proceed to fixing:

     a. **Transition ticket to fixing:**
        ```bash
        node .claude/skills/fixme/scripts/fixme-tools.cjs ticket transition <ticket-path> fixing
        ```

     b. **Dispatch fixer agent via Task tool:**
        ```
        First, read .claude/skills/fixme/agents/fix-agent.md for your role instructions.

        Fix this bug:
        - Ticket folder: <ticket-folder-dir>
        - Project context: .fixme/project-context.yaml
        ```

     c. **After fixer agent returns:**
        ALWAYS read ticket state from disk:
        ```bash
        node .claude/skills/fixme/scripts/fixme-tools.cjs ticket list <session-dir>
        ```
        Read the fixer's return summary.

     d. **Handle fixer result:**
        - If fixer returned "Fixed #NNNN: ..." (success):
          Report to user: "Fixed #NNNN: <summary>. Awaiting browser verification (Phase 5)."
          The ticket should now be in `fixing` state on success. Phase 5 will handle the final transition to done.
          NOTE: Actually re-read the ticket state from disk to determine actual state.
        - If fixer returned "Failed #NNNN: ..." (failure):
          Report to user: "Failed to fix #NNNN: <reason>. Moving to next ticket."
          The ticket is already in `failed` state (the fixer handles the transition and revert).

   - If agent returned "Investigated #NNNN: ..." with NOT_CONFIRMED/FAILED reproduction:
     Report findings to user. The investigation was inconclusive -- ask user if they want to:
     1. Skip this ticket and move to next
     2. Provide more details for a re-investigation
     If skip: transition to skipped with reason. If more details: keep ticket in investigating and wait.

   - If agent returned "BLOCKER #NNNN: ..." (environment blocker):
     Report the blocker to the user. Attempt recovery (see Browser Recovery below).
     If recovery succeeds, re-dispatch the investigation agent.
     If recovery fails, inform the user and wait for their guidance.
```

**2. Update path references for ticket-centric layout:**

Update step 3 in the Dispatch Loop (Create ticket asset directory):
- Old: `mkdir -p .fixme/sessions/<session>/assets/<ticket-number>/`
- New: Remove this step entirely. The ticket's `assets/` directory is created by `fixme-tools.cjs ticket create` automatically. Just pass the ticket folder's assets path to the investigation agent.

Update step 4 (investigation agent dispatch):
- Change the asset directory parameter from `.fixme/sessions/<session>/assets/<ticket-number>/` to `<ticket-folder>/assets/` where `<ticket-folder>` is `path.dirname(ticket-path)` (derived from the ticket.md path returned by `ticket next`).
- The dispatch prompt should use the ticket folder path:
  ```
  First, read .claude/skills/fixme/agents/investigation-agent.md for your role instructions.

  Then investigate this bug:
  - Ticket file: <ticket-path>
  - Project context: .fixme/project-context.yaml
  - Asset directory: <ticket-folder>/assets/
  - Dev server URL: <dev_server.url from project context>
  ```

Update Intake Dispatch Procedure step 3:
- The intake agent no longer receives a session assets directory. Instead, it uses the ticket's own assets dir.
- Change the Task prompt from `Session assets directory: <session-dir>/assets/` to `Ticket assets directory: <ticket-folder>/assets/` where ticket-folder is `path.dirname(ticket-path)`.

**3. Remove hardcoded model specifications:**

Remove `Use model: sonnet.` from the intake dispatch instructions (step 3 of Intake Dispatch Procedure).
Remove `Use model: opus.` from the investigation dispatch instructions (step 4 of Dispatch Loop).
Do NOT add `Use model: inherit.` -- the model is inherited automatically when no model is specified.

Add a brief note at the top of the Dispatch Loop section or in the Critical Rules:
```markdown
**Model inheritance:** Sub-agents inherit the orchestrator's model by default. Do not specify a model in Task dispatch prompts unless overriding for a specific reason.
```

**4. Update References section:**

Add the fixer agent reference:
```markdown
- **Fixer agent:** See `.claude/skills/fixme/agents/fix-agent.md` for the fixer coordinator's instructions, sub-agent dispatch, retry loop, and revert logic.
```
  </action>
  <verify>
Read the updated SKILL.md and verify:
1. Step 6 contains the fixer dispatch flow: transition to fixing, Task dispatch with fix-agent.md, result handling
2. No `model: opus` or `model: sonnet` strings anywhere in SKILL.md
3. Ticket asset directory references use `<ticket-folder>/assets/` pattern (not session-level assets)
4. Investigation dispatch uses new ticket-folder path pattern
5. Intake dispatch references ticket-level assets directory
6. References section includes fix-agent.md
7. No `mkdir -p` for asset directories (handled by ticket create)
8. The comment "(Phase 4 will add the investigating -> fixing transition here.)" is removed
  </verify>
  <done>
SKILL.md dispatch loop covers the full intake -> investigation -> fixing lifecycle. Fixer dispatched via Task tool with ticket folder path. Model inheritance applied (no hardcoded model values). All paths use ticket-centric layout. References include fixer agent.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md session and status flows for new directory layout</name>
  <files>.claude/skills/fixme/SKILL.md</files>
  <action>
Update remaining SKILL.md sections that reference the old directory layout.

**Session Start Flow (step 1):**
- `session create` no longer creates `tickets/` and `assets/` subdirectories (that's handled by the updated fixme-tools.cjs). No changes needed here unless there are explicit references to those directories.

**Status Query:**
- The `ticket list` command output now includes a `dir` field alongside `path`. The status table can remain the same (it only shows number, slug, state). No changes needed unless path formatting is referenced.

**Intake Dispatch Procedure:**
- Verify step 1 (pre-create ticket) still works. `ticket create` now creates NNNN-slug/ticket.md structure. The returned JSON includes `{ path, dir, number, slug, state }`. The `path` is the ticket.md path, `dir` is the ticket folder.
- Update step 3's Task prompt to pass `dir` (ticket folder) instead of deriving the assets path from the session directory.

**Session Control (Auto-Close, Graceful Stop):**
- `session summary` already updated in fixme-tools.cjs to scan for */ticket.md. No SKILL.md changes needed.

**Browser Recovery:**
- No path changes needed -- recovery is about browser/server state, not file paths.

**Verify the entire SKILL.md is consistent:** Read through the whole file and ensure NO references to the old `tickets/` or `assets/` directory patterns remain. Every path should use the ticket-centric layout: `<session>/<NNNN-slug>/ticket.md` and `<session>/<NNNN-slug>/assets/`.
  </action>
  <verify>
Grep the updated SKILL.md for old path patterns:
- No occurrences of `/tickets/` (the old flat directory)
- No occurrences of `<session>/assets/` or `<session-dir>/assets/` at the session level (assets are now per-ticket)
- Ensure `ticket create` output usage references both `path` and `dir` fields where appropriate
  </verify>
  <done>
All SKILL.md path references use ticket-centric layout. No remaining references to flat tickets/ or session-level assets/ directories. Intake, investigation, and fixer dispatch all pass correct ticket folder paths.
  </done>
</task>

</tasks>

<verification>
1. SKILL.md dispatch loop handles investigation -> fixing transition with fixer agent dispatch
2. No hardcoded model values anywhere in SKILL.md
3. All path references use ticket-centric layout (NNNN-slug/ticket.md, NNNN-slug/assets/)
4. Fixer dispatch passes only ticket folder path (orchestrator stays lean)
5. Orchestrator reads state from disk after fixer returns
6. References section includes all agent files (intake, investigation, fixer)
</verification>

<success_criteria>
- Dispatch loop covers full lifecycle: intake -> investigation -> fixing
- Orchestrator dispatches fixer agent via Task tool with ticket folder path
- No hardcoded model specifications remain
- All paths consistent with ticket-centric directory layout
- Orchestrator stays lean (passes paths, reads disk state, never reads ticket bodies)
</success_criteria>

<output>
After completion, create `.planning/phases/04-fix-commit/04-03-SUMMARY.md`
</output>
