---
phase: 02-intake-pipeline
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - ~/.claude/skills/fixme/SKILL.md
autonomous: true
requirements: [INTK-01, INTK-03, INTK-04, INTK-05]

must_haves:
  truths:
    - "Orchestrator pre-creates ticket with temporary slug, dispatches intake agent via Task tool, and relays the agent's summary to the user"
    - "/fixme:report submits a bug report and dispatches intake (bootstraps session if none active)"
    - "LLM intent detection classifies inline messages as bug reports with high/low/no confidence"
    - "Multiple concurrent bug report submissions are each captured as separate numbered tickets without loss or collision"
    - "Session auto-closes when all tickets processed and no intake agents running"
  artifacts:
    - path: "~/.claude/skills/fixme/SKILL.md"
      provides: "Updated orchestrator with intake dispatch flow, /fixme:report command, intent detection"
      contains: "intake-agent.md"
  key_links:
    - from: "SKILL.md intake dispatch"
      to: "fixme-tools.cjs ticket create"
      via: "Pre-creates ticket with --slug intake-tmp-<hex>"
      pattern: "ticket create.*intake-tmp"
    - from: "SKILL.md intake dispatch"
      to: "agents/intake-agent.md"
      via: "Task tool dispatch with ticket path and bug description"
      pattern: "intake-agent.md"
    - from: "SKILL.md intake failure"
      to: "fixme-tools.cjs ticket transition"
      via: "queued -> failed on intake agent failure"
      pattern: "ticket transition.*failed"
---

<objective>
Update SKILL.md to replace the direct ticket creation flow with the dispatch-based intake pipeline: pre-create-then-rename pattern, Task tool dispatch to intake agent, LLM intent detection for inline bug reports, `/fixme:report` command, and intake agent tracking.

Purpose: This transforms the orchestrator from a direct ticket creator into a coordinator that dispatches intake agents. Users can submit bugs via `/fixme:report` or inline text, and the orchestrator pre-creates a ticket (reserving the number atomically), dispatches an intake agent to process it, tracks the agent, and relays the result. This is the glue that connects the tooling (Plan 01) and the agent (Plan 02) into a working pipeline.

Output: Updated SKILL.md with complete intake dispatch flow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@~/.claude/skills/fixme/SKILL.md
@.planning/phases/02-intake-pipeline/02-RESEARCH.md
@.planning/phases/02-intake-pipeline/02-CONTEXT.md
@.planning/phases/02-intake-pipeline/02-01-SUMMARY.md
@.planning/phases/02-intake-pipeline/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update argument parsing and add /fixme:report command</name>
  <files>~/.claude/skills/fixme/SKILL.md</files>
  <action>
    Update the Argument Parsing table in SKILL.md to add the `report` sub-command:

    | Input | Sub-command | Session Name |
    |-------|-------------|--------------|
    | (empty) | start | auto-generated |
    | `start` | start | auto-generated |
    | `start my-session` | start | `my-session` |
    | `resume` | resume | most recent |
    | `resume my-session` | resume | `my-session` |
    | `status` | status | current/most recent |
    | `stop` | stop | current |
    | `report` | report | current (or bootstrap new) |
    | `report The login button is broken` | report | current (or bootstrap new) |

    Add a new section "## Report Flow" after "Session Start Flow" that handles `/fixme:report`:

    When sub-command is `report`:

    1. **Parse report text:** Everything after `report` in $ARGUMENTS is the bug description. If empty, ask user to describe the bug.

    2. **Ensure active session:**
       - Check for an active session via `session list`
       - If active session found: use it
       - If NO active session: bootstrap one (same as `start` flow -- create session, detect/load context), then continue with intake

    3. **Dispatch intake** (use the intake dispatch procedure defined in "Bug Intake" section below).

    4. **Enter dispatch loop** -- check for queued tickets ready for investigation.
  </action>
  <verify>
    SKILL.md contains "report" in the argument parsing table. SKILL.md contains "## Report Flow" section. The report flow handles both "report with text" and bare "report" (prompts for description). The report flow bootstraps a session if none active.
  </verify>
  <done>
    `/fixme:report <text>` is a recognized sub-command. It bootstraps a session if needed, dispatches intake, and enters the dispatch loop. Bare `/fixme:report` prompts for a description.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace Bug Intake section with dispatch-based intake pipeline</name>
  <files>~/.claude/skills/fixme/SKILL.md</files>
  <action>
    Replace the existing "## Bug Intake (In-Session)" section (currently lines 120-136) with a comprehensive dispatch-based intake flow. The new section should be:

    **## Bug Intake (In-Session)**

    **### Message Classification**

    When the user sends a message during an active session that is NOT a recognized command (start/resume/status/stop/report):

    1. **HIGH confidence bug report** -- dispatch intake immediately, no confirmation:
       - Problem descriptions: "X is broken", "X is not working", "X doesn't work"
       - Error reports: "I'm getting an error when...", "I see a 500 error on..."
       - Behavioral bugs: "When I do X, Y happens instead of Z"
       - Contains stack traces, error messages, or error screenshots
       - Visual bugs: "The button is overlapping the header", "The text is cut off"

    2. **LOW confidence** -- ask before dispatching:
       - Ambiguous messages that MIGHT be bug reports but could be conversation
       - Ask: "Is this a bug report you'd like me to track? (yes/no)"
       - If yes: dispatch intake with the original message as the bug description
       - If no: respond normally

    3. **NOT a bug report** -- respond normally:
       - Questions about status, progress, how things work
       - General conversation, clarifications about prior bugs
       - Feedback or comments that aren't actionable bugs

    **Principle:** Err on the side of asking when unsure. Mis-classifying a normal message as a bug report is worse than asking to confirm.

    **### Intake Dispatch Procedure**

    This procedure is used by both `/fixme:report` and inline bug detection:

    1. **Pre-create ticket with temporary slug:**
       ```bash
       node ~/.claude/skills/fixme/scripts/fixme-tools.cjs ticket create <session-dir> --slug intake-tmp-$(cat /dev/urandom | LC_ALL=C tr -dc 'a-f0-9' | head -c 4)
       ```
       Capture the output JSON: `{ path, number, slug, state }`.

    2. **Announce dispatch to user:** Single line, no ceremony:
       ```
       Intake dispatched for bug report (#NNNN)...
       ```

    3. **Dispatch intake agent via Task tool:**
       ```
       First, read ~/.claude/skills/fixme/agents/intake-agent.md for your role instructions.

       Then process this bug report:
       - Ticket file: <ticket-path from step 1>
       - Bug description: <verbatim user text, stripped of /fixme:report prefix if present>
       - Session assets directory: <session-dir>/assets/
       ```
       Use model: sonnet.

    4. **On Task return:**
       - If agent returned a summary (starts with "Queued #"): relay it to the user verbatim.
       - If agent returned an error or no summary: transition ticket to failed:
         ```bash
         node ~/.claude/skills/fixme/scripts/fixme-tools.cjs ticket transition <ticket-path> failed --reason "Intake agent failed: <error summary>"
         ```
         Inform user: "Intake failed for bug report (#NNNN). The report is preserved -- you can resubmit."

    5. **Continue dispatch loop.** Check for next queued ticket to investigate.

    **### Intake Agent Tracking**

    Maintain a mental list of dispatched intake agents and their ticket paths. This is NOT a persistent data structure -- it's the orchestrator's in-memory awareness of what's running.

    - On intake dispatch: note the ticket path
    - On intake return (success or failure): remove from tracking
    - Before auto-closing session: verify no intake agents are pending

    **### One Bug Per Message (v1)**

    If the user describes multiple bugs in one message, acknowledge all of them but create a ticket for the first bug only. Ask the user to submit the remaining bugs as separate messages.

    Also update the **Session Start Flow** section, step 3 ("Initial bug intake"):

    Replace:
    ```
    3. **Initial bug intake:**
       - If the user provided a bug report alongside the `/fixme` command: create the first ticket and begin the dispatch loop.
       - If no bug report was provided: inform the user the session is ready and ask them to describe a bug.
    ```

    With:
    ```
    3. **Initial bug intake:**
       - If the user provided a bug report alongside the `/fixme start` command: dispatch intake using the Intake Dispatch Procedure above, then enter the dispatch loop.
       - If no bug report was provided: inform the user the session is ready and ask them to describe a bug.
    ```

    Also update the **Session Control** section to add auto-close behavior:

    Add a subsection "### Auto-Close" before Graceful Stop:

    ```
    ### Auto-Close

    When the dispatch loop finds no queued tickets AND no intake agents are pending:
    1. Run session summary (same as graceful stop).
    2. Display the summary to the user.
    3. Session ends automatically -- no user action needed.
    ```

    Also update the **Dispatch Loop** step 1 to reference auto-close:

    Replace:
    ```
    If no queued tickets, prompt the user for a new bug report.
    ```
    With:
    ```
    If no queued tickets AND no intake agents are pending: auto-close the session (see Auto-Close).
    If no queued tickets BUT intake agents are still running: wait for intake to complete, then re-check.
    ```
  </action>
  <verify>
    SKILL.md "Bug Intake" section contains: Message Classification (HIGH/LOW/NOT), Intake Dispatch Procedure (5 steps with pre-create, Task dispatch, failure handling), Intake Agent Tracking, One Bug Per Message. Session Start references "Intake Dispatch Procedure". Auto-Close section exists. Dispatch Loop references auto-close.
  </verify>
  <done>
    The orchestrator's intake flow is fully dispatch-based: pre-creates ticket with temp slug, dispatches intake agent via Task tool, handles success/failure, tracks agents, auto-closes session. The old direct-creation flow is replaced. LLM intent detection classifies inline messages with HIGH/LOW/NOT confidence levels. `/fixme:report` and inline detection both use the same Intake Dispatch Procedure.
  </done>
</task>

</tasks>

<verification>
```bash
# SKILL.md updated
grep -c "intake-agent.md" ~/.claude/skills/fixme/SKILL.md
grep -c "intake-tmp" ~/.claude/skills/fixme/SKILL.md
grep -c "report" ~/.claude/skills/fixme/SKILL.md
grep -c "Message Classification" ~/.claude/skills/fixme/SKILL.md
grep -c "Auto-Close" ~/.claude/skills/fixme/SKILL.md

# Old direct-creation pattern removed
# Should NOT find "{VERBATIM_USER_REPORT}" pattern in Bug Intake section
grep -c "Write the user's verbatim report" ~/.claude/skills/fixme/SKILL.md  # should be 0
```
</verification>

<success_criteria>
- SKILL.md has /fixme:report as a recognized sub-command with session bootstrap
- Bug Intake section uses dispatch-based flow: pre-create, Task dispatch, failure handling
- LLM intent detection classifies messages as HIGH/LOW/NOT confidence
- Intake agent tracking documented (mental list, not persistent structure)
- Auto-close behavior: session ends when all tickets processed and no agents pending
- Old direct ticket creation flow is completely replaced
- One bug per message rule documented
- All existing sub-commands (start, resume, status, stop) still documented and functional
</success_criteria>

<output>
After completion, create `.planning/phases/02-intake-pipeline/02-03-SUMMARY.md`
</output>
