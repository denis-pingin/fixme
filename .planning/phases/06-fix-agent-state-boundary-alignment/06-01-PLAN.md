---
phase: 06-fix-agent-state-boundary-alignment
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - .claude/skills/fixme/scripts/fixme-tools.cjs
  - .claude/skills/fixme/scripts/fixme-tools.test.cjs
autonomous: true
requirements:
  - STAT-01
  - STAT-02

must_haves:
  truths:
    - "The TRANSITIONS constant has 9 states: queued, investigating, researching, planning, implementing, verifying, done, failed, skipped"
    - "The `fixing` state does not exist anywhere in fixme-tools.cjs"
    - "Transition `verifying -> planning` requires --reason and increments current_attempt"
    - "Transition `investigating -> researching` is valid"
    - "Transition `investigating -> fixing` is invalid (rejected by tool)"
    - "Cumulative durations are preserved when a state is re-entered (planning on retry)"
    - "Dead code (ticketDir function and its switch case) is removed"
  artifacts:
    - path: ".claude/skills/fixme/scripts/fixme-tools.cjs"
      provides: "Updated transition matrix, requiresReason, retry logic, cumulative durations"
      contains: "'researching'"
    - path: ".claude/skills/fixme/scripts/fixme-tools.test.cjs"
      provides: "Tests for new state transitions, retry path, invalid old transitions, cumulative durations"
      contains: "researching"
  key_links:
    - from: ".claude/skills/fixme/scripts/fixme-tools.test.cjs"
      to: ".claude/skills/fixme/scripts/fixme-tools.cjs"
      via: "execSync CLI invocation"
      pattern: "ticket transition.*researching"
---

<objective>
Replace the `fixing` state with three granular states (`researching`, `planning`, `implementing`) in fixme-tools.cjs, fix the cumulative durations bug, and remove dead code. All changes are TDD: write failing tests first, then implement.

Purpose: The TRANSITIONS constant is the foundation all agent files depend on. Every other plan in this phase builds on valid transitions being available in the CLI tool.
Output: Updated fixme-tools.cjs with new transition matrix and fixme-tools.test.cjs with comprehensive coverage of new states.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fix-agent-state-boundary-alignment/06-RESEARCH.md
@.claude/skills/fixme/scripts/fixme-tools.cjs
@.claude/skills/fixme/scripts/fixme-tools.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for new state transitions</name>
  <files>.claude/skills/fixme/scripts/fixme-tools.test.cjs</files>
  <action>
Add new test sections to fixme-tools.test.cjs BEFORE making any changes to fixme-tools.cjs. These tests define the target behavior:

**1. Happy path through new states:**
Create a ticket in queued state, then transition through the full chain:
`queued -> investigating -> researching -> planning -> implementing -> verifying -> done`
After each transition, verify the ticket file contains the correct `state:` value. At the end, verify the ticket has 6 entries in its transitions log.

**2. Retry path (verifying -> planning):**
Create a ticket, transition it to `verifying` (walk it through the happy path). Then transition `verifying -> planning` with `--reason "Build failed"`. Verify:
- Transition succeeds
- `current_attempt` incremented to 1
- The reason appears in the transitions log

**3. Invalid old transition (investigating -> fixing):**
Create a ticket in `investigating` state. Attempt `investigating -> fixing`. Verify:
- Command fails (non-zero exit)
- Error message mentions valid transitions from investigating

**4. New failure paths:**
Test that each new non-terminal state can transition to `failed`:
- `researching -> failed` (with --reason)
- `planning -> failed` (with --reason)
- `implementing -> failed` (with --reason)

**5. Cumulative durations on re-entry:**
Create a ticket. Walk it to `planning` state. Read the ticket and note the `planning.entered` timestamp. Then transition `planning -> implementing -> verifying`. Then transition `verifying -> planning --reason "..."`. Read the ticket's durations. Verify:
- `planning` duration entry has a NEW `entered` timestamp (different from first)
- The `planning` entry has a `prior_seconds` field OR the seconds value includes both visits

Note: For the cumulative durations test, the exact field name depends on implementation. Write the test to check that the seconds for `planning` after it exits the SECOND time are >= the seconds from the first visit. Use a helper that creates a ticket and walks it through states with small delays.

**6. Dead code removal test:**
Test that `ticket dir` subcommand returns an error (unknown subcommand) instead of succeeding.

**Update the existing `ticket dir` tests:** Change the two existing `ticket dir` tests so they expect FAILURE (unknown subcommand error) instead of success. Remove the `=== ticket dir tests ===` section header and replace with a single test that validates `ticket dir` is rejected.

Run the tests: `node .claude/skills/fixme/scripts/fixme-tools.test.cjs`
Confirm that the NEW tests fail (since fixme-tools.cjs hasn't been updated yet) while the EXISTING tests still pass (except the `ticket dir` tests which should now fail because the test expectations flipped but the code still has ticketDir).

Record which tests pass and which fail. This is the RED phase.
  </action>
  <verify>Run `node .claude/skills/fixme/scripts/fixme-tools.test.cjs`. New tests should fail. Count of failing tests should be > 0. Existing tests (create, list, next, rename, session) should still pass.</verify>
  <done>Test file has 6+ new test cases covering: happy path with new states, retry verifying->planning, invalid investigating->fixing, new failure paths, cumulative durations, dead code removal. Tests fail for the right reasons (invalid transitions, missing states).</done>
</task>

<task type="auto">
  <name>Task 2: Implement state machine changes and fix all tests</name>
  <files>.claude/skills/fixme/scripts/fixme-tools.cjs</files>
  <action>
Update fixme-tools.cjs to make all tests pass. Changes in order:

**1. Replace TRANSITIONS constant (line ~529-537):**
```javascript
const TRANSITIONS = {
  'queued':         ['investigating', 'skipped', 'failed'],
  'investigating':  ['researching', 'skipped', 'failed'],
  'researching':    ['planning', 'failed'],
  'planning':       ['implementing', 'failed'],
  'implementing':   ['verifying', 'failed'],
  'verifying':      ['done', 'planning', 'failed'],
  'done':           [],
  'failed':         [],
  'skipped':        [],
};
```

**2. Update requiresReason (line ~542-547):**
Change `fromState === 'verifying' && toState === 'investigating'` to `fromState === 'verifying' && toState === 'planning'`.

**3. Update current_attempt increment (line ~722-725):**
Change `currentState === 'verifying' && newState === 'investigating'` to `currentState === 'verifying' && newState === 'planning'`.

**4. Fix cumulative durations bug (line ~703-709):**
Before overwriting `durations[newState]`, check if a prior entry exists with a `seconds` value. If so, preserve it as `prior_seconds` and add it to the new entry:

```javascript
// Preserve cumulative seconds for states visited multiple times (e.g., planning on retry)
const priorSeconds = (durations[newState] && durations[newState].seconds) ? durations[newState].seconds : 0;
const priorAccumulated = (durations[newState] && durations[newState].prior_seconds) ? durations[newState].prior_seconds : 0;
durations[newState] = { entered: now };
if (priorSeconds + priorAccumulated > 0) {
  durations[newState].prior_seconds = priorSeconds + priorAccumulated;
}
```

When computing exit seconds later, the total cumulative time will be `seconds + prior_seconds`.

**5. Remove dead code -- ticketDir function (line ~890-895):**
Delete the `ticketDir` function entirely.

**6. Remove ticketDir from switch statement (line ~1403-1404):**
Delete the `case 'dir':` and `return ticketDir(args[0]);` lines.

**7. Update the default error message (line ~1406):**
Change `Valid: create, transition, list, next, rename, dir` to `Valid: create, transition, list, next, rename`.

Run all tests: `node .claude/skills/fixme/scripts/fixme-tools.test.cjs`
All tests (old and new) must pass. This is the GREEN phase.
  </action>
  <verify>Run `node .claude/skills/fixme/scripts/fixme-tools.test.cjs`. ALL tests pass (0 failures). Also verify: `grep -c "fixing" .claude/skills/fixme/scripts/fixme-tools.cjs` returns 0 (no references to the old `fixing` state remain in the code).</verify>
  <done>fixme-tools.cjs has the new 9-state transition matrix, updated retry logic (verifying->planning), cumulative durations fix, and dead code removed. All tests pass green. No references to `fixing` state remain in the file.</done>
</task>

</tasks>

<verification>
1. `node .claude/skills/fixme/scripts/fixme-tools.test.cjs` -- all tests pass
2. `grep "fixing" .claude/skills/fixme/scripts/fixme-tools.cjs` -- no matches (state completely removed)
3. `grep "'researching'" .claude/skills/fixme/scripts/fixme-tools.cjs` -- matches in TRANSITIONS
4. `grep "ticket dir" .claude/skills/fixme/scripts/fixme-tools.cjs` -- no matches (dead code removed)
</verification>

<success_criteria>
- TRANSITIONS constant has exactly 9 keys: queued, investigating, researching, planning, implementing, verifying, done, failed, skipped
- `fixing` state does not appear in fixme-tools.cjs
- `ticketDir` function and its switch case are removed
- Cumulative durations work correctly across state re-entry
- All existing + new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-fix-agent-state-boundary-alignment/06-01-SUMMARY.md`
</output>
