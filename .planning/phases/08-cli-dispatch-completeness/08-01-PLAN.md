---
phase: 08-cli-dispatch-completeness
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - .claude/skills/fixme/scripts/fixme-tools.cjs
  - .claude/skills/fixme/scripts/fixme-tools.test.cjs
  - .claude/skills/fixme/SKILL.md
  - .claude/skills/fixme/templates/session.md
autonomous: true
requirements: [FIXR-01, STAT-03, FIXR-03, SYST-04]
gap_closure: true

must_haves:
  truths:
    - "ticket list CLI output includes files_changed array for each ticket"
    - "SKILL.md commit step can retrieve files_changed from ticket list without reading ticket body"
    - "SKILL.md investigation-agent and fix-agent Task dispatches specify subagent_type explicitly"
    - "Active intake agent tracking survives context compaction via session file persistence"
  artifacts:
    - path: ".claude/skills/fixme/scripts/fixme-tools.cjs"
      provides: "ticketList returns files_changed from frontmatter"
      contains: "files_changed"
    - path: ".claude/skills/fixme/scripts/fixme-tools.test.cjs"
      provides: "Tests verifying files_changed in ticketList output"
      contains: "files_changed"
    - path: ".claude/skills/fixme/SKILL.md"
      provides: "subagent_type on investigation/fix dispatches, intake persistence via session file"
      contains: "subagent_type"
    - path: ".claude/skills/fixme/templates/session.md"
      provides: "active_intakes field in session template"
      contains: "active_intakes"
  key_links:
    - from: ".claude/skills/fixme/scripts/fixme-tools.cjs"
      to: "ticket template frontmatter"
      via: "parseFrontmatter reads files_changed array"
      pattern: "files_changed.*fm\\.files_changed"
    - from: ".claude/skills/fixme/SKILL.md"
      to: "ticket list output"
      via: "commit step reads files_changed from ticket list JSON"
      pattern: "files_changed"
    - from: ".claude/skills/fixme/SKILL.md"
      to: "session.md file"
      via: "read/write active_intakes array for compaction safety"
      pattern: "active_intakes"
---

<objective>
Close all Phase 8 audit gaps: expose files_changed in ticketList CLI output, add subagent_type to SKILL.md Task dispatches, and persist intake agent tracking to the session file for context compaction safety.

Purpose: The SKILL.md commit step needs files_changed from `ticket list` to stage files for git add (INT-11). Task dispatches need explicit subagent_type for reliable tool propagation. Intake tracking must survive compaction to prevent premature auto-close.
Output: Updated fixme-tools.cjs with files_changed in ticketList, tests, SKILL.md with subagent_type and intake persistence, session template with active_intakes field.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/fixme/scripts/fixme-tools.cjs
@.claude/skills/fixme/scripts/fixme-tools.test.cjs
@.claude/skills/fixme/SKILL.md
@.claude/skills/fixme/templates/session.md
@.claude/skills/fixme/templates/ticket.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD -- files_changed in ticketList output</name>
  <files>.claude/skills/fixme/scripts/fixme-tools.cjs, .claude/skills/fixme/scripts/fixme-tools.test.cjs</files>
  <action>
**RED phase:**

Add tests to fixme-tools.test.cjs in a new test suite section "=== ticket list: files_changed field ===":

1. **Test "list: includes files_changed from frontmatter"**: Create a ticket folder using `createTicketFolder`. After creation, manually edit the ticket.md file to set `files_changed: ["src/app.tsx", "src/utils.ts"]` in the frontmatter (use string replace on the content). Run `ticket list` and assert that `result.data[0].files_changed` is an array with exactly those two values.

2. **Test "list: files_changed defaults to empty array when not in frontmatter"**: Create a ticket folder. Manually remove the `files_changed: []` line from the ticket.md frontmatter entirely. Run `ticket list` and assert `result.data[0].files_changed` is an empty array `[]`.

3. **Test "list: files_changed with empty array returns empty array"**: Create a ticket folder (default template has `files_changed: []`). Run `ticket list` and assert `result.data[0].files_changed` is `[]`.

Run tests -- these 3 new tests MUST fail (ticketList currently returns `{ number, slug, state, title, path, dir }` with no files_changed).

Commit: `test(08-01): add failing tests for files_changed in ticketList`

**GREEN phase:**

In fixme-tools.cjs, modify the `ticketList` function (around line 778). In the `entries.map` callback where the return object is built, add `files_changed` to the returned object:

```javascript
return {
  number: fm.number || d.match(/^(\d+)-/)?.[1] || '0000',
  slug,
  state: fm.state || 'unknown',
  title: extractTitle(body, slug),
  files_changed: Array.isArray(fm.files_changed) ? fm.files_changed : [],
  path: ticketPath,
  dir: path.join(sessionDir, d),
};
```

The key addition is `files_changed: Array.isArray(fm.files_changed) ? fm.files_changed : []`. This reads the `files_changed` array from frontmatter (already parsed by `parseFrontmatter` which handles inline arrays like `[a, b]` and multiline arrays). Falls back to empty array if missing or not an array.

Run tests -- all tests must pass (existing 36 + 3 new = 39 total).

Commit: `feat(08-01): expose files_changed in ticketList output`
  </action>
  <verify>Run `node .claude/skills/fixme/scripts/fixme-tools.test.cjs` -- all tests pass, 0 failures. The 3 new files_changed tests appear in output as PASS.</verify>
  <done>ticketList returns files_changed array for each ticket, sourced from frontmatter. Empty array when field is missing or empty. All 39+ tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: SKILL.md -- subagent_type on Task dispatches + commit step confirmation</name>
  <files>.claude/skills/fixme/SKILL.md</files>
  <action>
**2a. Add subagent_type to investigation-agent dispatch (step 2 of Dispatch Loop, around line 181):**

The current Task dispatch prompt for investigation-agent (lines 181-188) does NOT specify subagent_type. Add `subagent_type: "general-purpose"` to the dispatch. The updated dispatch block should look like:

```
   First, read ~/.claude/skills/fixme/agents/investigation-agent.md for your role instructions.

   Then investigate this bug:
   - Ticket file: <ticket-path>
   - Project context: .fixme/project-context.yaml
   - Asset directory: <ticket-folder>/assets/
   - Dev server URL: <dev_server.url from project context>
```

And the Task tool call itself should include `subagent_type: "general-purpose"`. Add a note ABOVE the dispatch prompt block:

```
   Dispatch investigation agent via Task tool (use `subagent_type: "general-purpose"`):
```

Replace the existing line "**Dispatch investigation agent via Task tool:**" (line 178) with "**Dispatch investigation agent via Task tool (use `subagent_type: "general-purpose"`):**"

**2b. Add subagent_type to fix-agent dispatch (step 4a, around line 209):**

Same pattern. Replace "**Dispatch fix-agent via Task tool:**" with "**Dispatch fix-agent via Task tool (use `subagent_type: "general-purpose"`):**"

**2c. Add subagent_type to intake-agent dispatch (Intake Dispatch Procedure step 3, around line 363):**

Replace "**Dispatch intake agent via Task tool:**" with "**Dispatch intake agent via Task tool (use `subagent_type: "general-purpose"`):**"

**2d. Verify commit step wiring (step 4.c.1, lines 233-238):**

Read the commit step. It already says:
- Read `files_changed` and `title` from `ticket list` output
- Stage files from `files_changed`

This is ALREADY correctly wired to consume the files_changed field from ticketList (fixed in Task 1). No changes needed to the commit step itself. Just verify the text is correct and consistent.

Commit: `feat(08-01): add subagent_type to SKILL.md Task dispatches`
  </action>
  <verify>Grep SKILL.md for "subagent_type" -- should find 3 occurrences (investigation, fix-agent, intake dispatches). Grep for "files_changed" -- should find 2 occurrences (commit step references). No broken markdown formatting.</verify>
  <done>All three SKILL.md Task dispatches (investigation-agent, fix-agent, intake-agent) specify subagent_type: "general-purpose" explicitly. Commit step correctly references files_changed from ticket list.</done>
</task>

<task type="auto">
  <name>Task 3: Persist intake agent tracking to session file</name>
  <files>.claude/skills/fixme/SKILL.md, .claude/skills/fixme/templates/session.md</files>
  <action>
**3a. Add active_intakes field to session template:**

In `.claude/skills/fixme/templates/session.md`, add `active_intakes: []` to the frontmatter, after `tickets_total: 0` and before the closing `---`. The updated frontmatter should be:

```yaml
---
name: "{SESSION_NAME}"
created: "{TIMESTAMP}"
status: active
completed:
duration_seconds:
tickets_done: 0
tickets_failed: 0
tickets_skipped: 0
tickets_total: 0
active_intakes: []
---
```

**3b. Rewrite SKILL.md "Intake Agent Tracking" section (lines 384-390):**

Replace the current section which says "Maintain a mental list" with a disk-persisted approach. The new section:

```markdown
### Intake Agent Tracking

Track dispatched intake agents in the session file's `active_intakes` frontmatter field. This persists across context compaction.

- **On intake dispatch** (after step 1 of Intake Dispatch Procedure): Add the ticket path to the session file's `active_intakes` array. Use the Edit tool to append the path to the YAML array in session.md frontmatter.
- **On intake return** (step 4 of Intake Dispatch Procedure, success or failure): Remove the ticket path from `active_intakes` in session.md frontmatter using the Edit tool.
- **Before auto-closing session:** Read session.md frontmatter. If `active_intakes` is non-empty, intake agents are still pending -- wait for them before closing.
- **On session resume:** Read `active_intakes` from session.md. For each ticket path listed, check if the ticket is still in `queued` state (via `ticket list`). If a ticket has moved past `queued`, its intake completed during a previous context -- remove it from `active_intakes`. If still `queued`, the intake may still be running or may have crashed -- treat as pending.
```

**3c. Update auto-close check (around line 175-176):**

The current text says: "If no queued tickets AND no intake agents are pending: auto-close the session"

Update this to be explicit about reading from disk: "If no queued tickets AND `active_intakes` in session.md frontmatter is empty: auto-close the session (see Auto-Close)."

And the next line: "If no queued tickets BUT intake agents are still running: wait for intake to complete, then re-check." becomes: "If no queued tickets BUT `active_intakes` in session.md is non-empty: wait for intake to complete, then re-check."

Commit: `feat(08-01): persist intake tracking to session file for compaction safety`
  </action>
  <verify>Read session template -- has `active_intakes: []` in frontmatter. Grep SKILL.md for "active_intakes" -- should find references in Intake Agent Tracking section, dispatch loop auto-close check, and intake dispatch procedure. Grep for "mental list" -- should return 0 results (old pattern removed).</verify>
  <done>Intake agent tracking persisted to session.md's active_intakes frontmatter field. Session template includes the field. SKILL.md references disk state for compaction safety. No in-memory-only tracking remains.</done>
</task>

</tasks>

<verification>
1. `node .claude/skills/fixme/scripts/fixme-tools.test.cjs` -- all tests pass (39+ total, 0 failures)
2. `grep -c "subagent_type" .claude/skills/fixme/SKILL.md` returns 3
3. `grep -c "files_changed" .claude/skills/fixme/scripts/fixme-tools.cjs` confirms field in ticketList
4. `grep -c "active_intakes" .claude/skills/fixme/SKILL.md` returns 4+ (tracking section + dispatch loop + resume)
5. `grep -c "active_intakes" .claude/skills/fixme/templates/session.md` returns 1
6. `grep -c "mental list" .claude/skills/fixme/SKILL.md` returns 0 (old pattern removed)
</verification>

<success_criteria>
- ticket list CLI returns files_changed for each ticket (INT-11 closed)
- SKILL.md commit step can use files_changed from ticket list without reading ticket body (FIXR-01, STAT-03)
- All three SKILL.md Task dispatches include subagent_type: "general-purpose" (FIXR-03)
- Intake tracking persists to session file, surviving context compaction (SYST-04)
- All existing + new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-cli-dispatch-completeness/08-01-SUMMARY.md`
</output>
