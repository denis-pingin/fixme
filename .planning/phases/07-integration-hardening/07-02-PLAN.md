---
phase: 07-integration-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/fixme/agents/fix-agent.md
  - .claude/skills/fixme/agents/fix-researcher.md
  - .claude/skills/fixme/agents/fix-planner.md
  - .claude/skills/fixme/agents/fix-implementer.md
  - .claude/skills/fixme/agents/investigation-agent.md
autonomous: true
requirements: [FIXR-05, STAT-03, BROW-02]
gap_closure: true

must_haves:
  truths:
    - "fix-agent.md implementer dispatch on retry (attempt 2+) passes verification report path"
    - "Only one writer (sub-agents) writes bullets to the ticket Fix section -- fix-agent writes structured attempt/phase bullets only"
    - "investigation-agent.md example paths use flat assets/ directory, not assets/<ticket-number>/ subdirectory"
  artifacts:
    - path: ".claude/skills/fixme/agents/fix-agent.md"
      provides: "Retry-aware implementer dispatch with verification report path"
      contains: "Verifier feedback"
    - path: ".claude/skills/fixme/agents/fix-researcher.md"
      provides: "Researcher without duplicate fix section writes"
    - path: ".claude/skills/fixme/agents/fix-planner.md"
      provides: "Planner without duplicate fix section writes"
    - path: ".claude/skills/fixme/agents/fix-implementer.md"
      provides: "Implementer without duplicate fix section writes"
    - path: ".claude/skills/fixme/agents/investigation-agent.md"
      provides: "Correct flat assets/ path in example"
      contains: "assets/repro-"
  key_links:
    - from: ".claude/skills/fixme/agents/fix-agent.md"
      to: ".claude/skills/fixme/agents/fix-implementer.md"
      via: "Task dispatch prompt with verification report path on retry"
      pattern: "Verifier feedback.*verify-"
    - from: ".claude/skills/fixme/agents/fix-agent.md"
      to: "ticket.md Fix section"
      via: "Edit tool appending structured bullets (sole writer of attempt/phase format)"
      pattern: "Edit.*Fix"
---

<objective>
Fix agent MD files to close three integration gaps: pass verification feedback to implementer on retry (INT-07), remove duplicate Fix section writes from sub-agents (INT-09), and correct assets path in investigation-agent example (INT-10).

Purpose: Closes INT-07, INT-09, INT-10. The implementer currently gets no retry feedback. Both sub-agents and fix-agent write to the Fix section causing duplicates. Investigation-agent references a non-existent subdirectory path.

Output: Updated agent MD files with consistent write ownership and correct paths.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/fixme/agents/fix-agent.md
@.claude/skills/fixme/agents/fix-researcher.md
@.claude/skills/fixme/agents/fix-planner.md
@.claude/skills/fixme/agents/fix-implementer.md
@.claude/skills/fixme/agents/investigation-agent.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add retry-aware implementer dispatch and remove sub-agent Fix section writes</name>
  <files>
    .claude/skills/fixme/agents/fix-agent.md
    .claude/skills/fixme/agents/fix-researcher.md
    .claude/skills/fixme/agents/fix-planner.md
    .claude/skills/fixme/agents/fix-implementer.md
  </files>
  <action>
    **INT-07: Retry-aware implementer dispatch in fix-agent.md**

    In fix-agent.md, section "4b. Dispatch Fix-Implementer" (lines ~108-129), the dispatch prompt is static for all attempts. Add a retry variant similar to how section 4a already has first-attempt vs retry variants for the planner.

    Replace the single dispatch block with two variants:

    **First attempt:**
    ```
    First, read ~/.claude/skills/fixme/agents/fix-implementer.md for your role instructions.

    Implement the fix:
    - Ticket folder: <ticket-folder>
    - Plan file: <path to current plan file>
    - Project context: .fixme/project-context.yaml
    - Verifier feedback: "first attempt -- no prior verification"
    ```

    **Retry (attempt 2+):**
    ```
    First, read ~/.claude/skills/fixme/agents/fix-implementer.md for your role instructions.

    Implement the fix:
    - Ticket folder: <ticket-folder>
    - Plan file: <path to current plan file>
    - Project context: .fixme/project-context.yaml
    - Verifier feedback: <path to last verification report from verifications/ directory>
    - Previous failure: "<1-2 sentence summary from verifier findings>"
    ```

    The verification report path comes from the previous iteration's step 4c where fix-agent already reads the report. Store the last verification report path across loop iterations.

    **INT-09: Remove sub-agent Fix section writes**

    The fix-agent.md already writes structured bullets like `- **Research** (42s) -> research/NNNN-research.md` after each sub-agent returns. The sub-agents ALSO write their own bullets in a different format (`- **Research:** <path> -- <summary>`). This causes duplicates.

    Decision: **Remove sub-agent writes. fix-agent is the sole writer of Fix section bullets.** The sub-agents produce artifact files (research/*.md, plans/*.md, verifications/*.md) which is their primary output. The fix-agent summarizes them into the ticket.

    In each of these three files, DELETE the "Final Step: Record Summary in Ticket" section entirely:
    - `fix-researcher.md` lines ~101-104: Delete the section starting with `### Final Step: Record Summary in Ticket` through the bullet format line
    - `fix-planner.md` lines ~95-98: Delete the same section
    - `fix-implementer.md` lines ~69-72: Delete the same section

    Keep the "Return Summary" section that follows -- those one-liner returns are NOT the same as the ticket writes.
  </action>
  <verify>
    Read each modified file and confirm:
    1. fix-agent.md has two implementer dispatch variants (first attempt vs retry with verification path)
    2. fix-researcher.md has NO "Final Step: Record Summary in Ticket" section
    3. fix-planner.md has NO "Final Step: Record Summary in Ticket" section
    4. fix-implementer.md has NO "Final Step: Record Summary in Ticket" section
    5. All "Return Summary" sections remain intact
  </verify>
  <done>
    fix-agent.md implementer dispatch passes verification report path on retry. Sub-agents no longer write to the Fix section. fix-agent is the sole writer of Fix section structured bullets.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix investigation-agent assets path to use flat directory</name>
  <files>
    .claude/skills/fixme/agents/investigation-agent.md
  </files>
  <action>
    **INT-10: Assets path format inconsistency**

    In investigation-agent.md, line ~135, the example shows:
    ```
    - Screenshot: `assets/<ticket-number>/repro-<description>.png`
    ```

    But `ticketCreate` creates a flat `assets/` directory inside the ticket folder (no per-ticket subdirectory under assets). The correct path is:
    ```
    - Screenshot: `assets/repro-<description>.png`
    ```

    Use Edit to replace the incorrect path pattern with the correct flat path. The ticket folder already IS the per-ticket directory (`NNNN-slug/`), so `NNNN-slug/assets/` is the correct location -- no need for `assets/<ticket-number>/` nesting.
  </action>
  <verify>
    Read investigation-agent.md and confirm the screenshot path example uses `assets/repro-<description>.png` without the `<ticket-number>/` subdirectory.
  </verify>
  <done>
    investigation-agent.md example paths match the actual flat `assets/` directory structure created by `ticketCreate`.
  </done>
</task>

</tasks>

<verification>
Read all 5 modified files and verify:
1. fix-agent.md: implementer dispatch has first-attempt and retry variants
2. fix-researcher.md, fix-planner.md, fix-implementer.md: no "Record Summary in Ticket" sections
3. investigation-agent.md: assets path is flat (no ticket-number subdirectory)
</verification>

<success_criteria>
1. Implementer on retry receives verification report path so it knows what failed
2. No duplicate bullet writes -- fix-agent is sole Fix section writer
3. Investigation-agent path example matches actual directory layout
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration-hardening/07-02-SUMMARY.md`
</output>
